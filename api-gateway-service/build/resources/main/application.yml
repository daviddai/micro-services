spring:
  application:
    name: api-gateway-service-client
server:
  port: 8084
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://192.168.1.7:8761/eureka}
  instance:
    preferIpAddress: true